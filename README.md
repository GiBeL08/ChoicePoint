# 🎯 ChoicePoint — Платформа для Дилемм

**Помогайте друг другу принимать сложные решения!**

ChoicePoint — это современная веб-платформа, где пользователи могут создавать дилеммы, голосовать по ним и видеть результаты. Все новые дилеммы проходят модерацию администраторов.

## 📊 О проекте

- **Язык**: PHP 7.4+, MySQL 5.7+, Vanilla JS (ES6)
- **Стиль**: Современный UI с градиентами (индиго #6366f1 и розовый #ec4899)
- **Функции**: Регистрация, создание дилемм, голосование, лайки/дизлайки, модерация
- **Безопасность**: Хеширование паролей (bcrypt), защита от SQL-инъекций, role-based доступ
- **Статус**: Полностью функционален

## ✨ Возможности

**Для всех:**
- Просмотр одобренных дилемм с результатами
- Быстрая регистрация и вход
- Адаптивный дизайн (мобильный, планшет, ПК)

**Для пользователей:**
- Создание новых дилемм (требуется модерация)
- Одноразовое голосование по вопросам
- Лайки/дизлайки через AJAX
- Добавление описания и категории

**Для администраторов:**
- Модерация (одобрение/отклонение) дилемм
- Редактирование и удаление контента
- Просмотр всех дилемм

## 🔧 Технический стек

- **Backend**: PHP 7.4+ (процедурный + контроллеры)
- **БД**: MySQL 5.7+ (PDO)
- **Frontend**: HTML5, CSS3 (Flexbox), Vanilla JS (ES6)
- **Безопасность**: 
  - password_hash (bcrypt)
  - prepared statements (PDO)
  - htmlspecialchars (XSS защита)
  - role-based доступ

## 📁 Структура

```
/
├── index.php                    # Главная (список дилемм)
├── logout.php, vote.php, reaction.php, comment.php
├── check_db.php                 # Проверка и создание БД
├── config/database.php          # Конфигурация PDO
├── public/
│   ├── style.css                # 506 строк CSS (градиенты, адаптив)
│   ├── script.js                # AJAX функции
│   ├── login.php, register.php  # Аутентификация
│   ├── add_question.php         # Создание дилеммы
│   └── admin/
│       ├── index.php            # Админ-панель
│       ├── moderation.php       # Модерация
│       ├── edit_question.php    # Редактирование
│       └── delete_question.php
├── app/
│   ├── Controllers/
│   │   ├── AuthController.php   # Логика регистрации/входа
│   │   └── QuestionController.php # Дилеммы и голосование (284 строк)
│   ├── helpers/
│   │   ├── is_admin.php
│   │   └── Security.php
│   ├── Models/
│   └── Views/
│       └── admin_nav.php
└── Views/partials/
    ├── header.php
    └── footer.php
```

## 🚀 Установка

### 1. Создать БД
```sql
CREATE DATABASE choicepoint CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 2. Конфигурация
Отредактируйте `config/database.php`:
```php
$host = 'localhost';
$db   = 'choicepoint';
$user = 'root';
$pass = '';
```

### 3. Инициализация
Откройте в браузере: `http://localhost/check_db.php` (создаст таблицы)

### 4. Запуск
```bash
php -S localhost:8000
# или через XAMPP (скопируйте в htdocs)
```

Откройте: `http://localhost/` или `http://localhost:8000/`

## 📋 QuestionController API

- `getActiveQuestions()` - одобренные дилеммы
- `getPendingQuestions()` - дилеммы на модерации
- `vote($user_id, $question_id, $option_id)` - добавить голос
- `getUserVote($user_id, $question_id)` - получить голос
- `getResults($question_id)` - результаты голосования
- `addQuestion($user_id, $title, $options, $description, $category)` - новая дилемма
- `deleteQuestion($id)` - удалить (админ)
- `approveQuestion($id)` - одобрить (админ)

## AuthController API

- `register($email, $password)` - регистрация
- `login($email, $password)` - вход
- `logout()` - выход

## 🔐 Безопасность

✅ Хеширование паролей (bcrypt)
✅ Prepared statements (защита от SQL-инъекций)
✅ Санитизация вывода (htmlspecialchars)
✅ Role-based доступ (admin/user)
✅ Уникальные ограничения в БД
✅ Проверка сессии и user_id

## 🎨 Цвета

| Элемент | Цвет | Код |
|---------|------|-----|
| Основной | Indigo | #6366f1 |
| Темный | Dark Indigo | #4f46e5 |
| Светлый | Light Indigo | #818cf8 |
| Вторичный | Pink | #ec4899 |
| Успех | Green | #10b981 |
| Ошибка | Red | #ef4444 |
| Предупреждение | Amber | #f59e0b |

## 📊 Схема БД

**users**: id, email, password, role, created_at
**questions**: id, user_id, title, description, category, is_approved, is_active, created_at
**options**: id, question_id, text
**votes**: id, user_id, question_id, option_id, created_at
**question_reactions**: id, user_id, question_id, reaction_type, created_at

## 📝 Валидация

- Email: уникальный, валидный формат
- Пароль: минимум 6 символов
- Название: 3-120 символов
- Варианты: 3-60 символов каждый, 2-5 вариантов

## 🐛 Решение проблем

**Лайки не работают?**
→ Откройте http://localhost/check_db.php

**БД не подключается?**
1. Проверьте MySQL запущен
2. Проверьте config/database.php
3. Откройте http://localhost/check_db.php

**Стили не загружаются?**
→ Очистите кэш (Ctrl+Shift+Del) и перезагрузите

**Страницы пусты?**
→ Проверьте браузерную консоль (F12)

## 💡 Советы

- http://localhost/check_db.php для диагностики
- phpMyAdmin для инспекции БД
- Проверяйте консоль браузера (F12) для JS ошибок
- Логи PHP в XAMPP/logs/

## 🎯 Использование

1. Зарегистрируйтесь
2. Создавайте дилеммы (требуется модерация администратора)
3. Голосуйте по дилеммам других пользователей
4. Используйте лайки/дизлайки для оценки

## 👨‍💼 Администрирование

1. Войдите как админ
2. Перейдите в админ-панель
3. Модерируйте дилеммы (одобрение/отклонение)
4. Редактируйте или удаляйте контент

## 🎁 Особенности

- AJAX для голосования и реакций (без перезагрузки)
- Система модерации для контроля качества
- Одноразовое голосование (INSERT IGNORE)
- Плавные анимации CSS3
- Прогресс-бары для результатов
- Доступная мобильная навигация

---

**ChoicePoint** — Помогайте друг другу принимать решения! 🎯

Версия: 2.0
Последнее обновление: 28 декабря 2025
